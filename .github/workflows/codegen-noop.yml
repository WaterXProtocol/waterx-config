name: Codegen freshness

# Companion to codegen.yml: the SAME job name (`regen`) on the INVERSE path
# filter, so `regen` can be a required check without deadlocking PRs that
# touch no generated inputs (a path-skipped required check sits at
# "Expected — Waiting for status" forever; review finding). Keep this
# paths-ignore list identical to codegen.yml's paths list — BOTH workflow
# files appear in BOTH lists, so a PR editing either one always runs the REAL
# regeneration job and never a modified no-op (self-certification finding).
on:
  pull_request:
    paths-ignore: [schema/**, scripts/**, packages/**, docs/FIELDS.md, mainnet.json, testnet.json, .github/workflows/codegen.yml, .github/workflows/codegen-noop.yml]

concurrency:
  group: codegen-noop-${{ github.ref }}
  cancel-in-progress: true

jobs:
  regen:
    runs-on: ubuntu-latest
    steps:
      - run: echo "No generated inputs touched — codegen freshness trivially holds."
